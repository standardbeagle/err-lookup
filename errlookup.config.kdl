provider "claude" {
    command "claude"
    args "-p" "--output-format" "json" "--max-turns" "30"
    timeout-ms 600000
}
defaults {
    primary "claude"
    max-concurrent 1
    delay-between-phases-ms 5000
}
